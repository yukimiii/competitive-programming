#include <bits/stdc++.h>
#define FOR(i, a, n) for (int i = a; i < n; i++)
#define REP(w, n) FOR(w, 0, n)
using namespace std;
typedef long long ll;

int main()
{
	int n, d;
	ll ans = 0;
	vector<ll> a(n);
	REP(i, n) { cin >> a[i]; }
	REP(i, (1 << n))
	{
		ll bit = 0;
	}
	return (0);
}
